{
  "config": {
    "step": {
      "user": {
        "title": "myAI configuration",
        "description": "Configure a myAI instance. You can add multiple instances with different models and API keys.",
        "data": {
          "name": "Name (e.g. Qwen 3.5)",
          "api_key": "API key",
          "base_url": "Base URL",
          "model": "Model ID"
        }
      }
    },
    "error": {
      "cannot_connect": "Connection failed – check the URL or API key",
      "invalid_auth": "Invalid API key",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This instance is already configured"
    }
  }
}
